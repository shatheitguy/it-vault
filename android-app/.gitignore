*.iml
.gradle/
build/
local.properties
.idea/
.DS_Store
captures/
.externalNativeBuild/
.cxx/
# Signing material — NEVER commit. The whole keystore/ dir is excluded so that
# backups and *.properties variants can't slip through on a suffix.
keystore/
keystore.properties
keystore.properties.*
*.keystore
*.jks
*.apk
